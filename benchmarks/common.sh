#!/usr/bin/env bash

set -euo pipefail

BENCHMARKS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${BENCHMARKS_DIR}/.." && pwd)"

PYTHON_BIN="${PYTHON_BIN:-${REPO_ROOT}/.venv/bin/python}"
MODEL_PATH="${MODEL_PATH:-${REPO_ROOT}/ckpt/rwkv-23552}"
TOKENIZER_PATH="${TOKENIZER_PATH:-${MODEL_PATH}}"
HOST="${HOST:-127.0.0.1}"
PORT="${PORT:-31000}"
BASE_URL="${BASE_URL:-http://${HOST}:${PORT}}"

export SGLANG_DISABLE_CUDNN_CHECK="${SGLANG_DISABLE_CUDNN_CHECK:-1}"
export SGLANG_TORCH_PROFILER_DIR="${SGLANG_TORCH_PROFILER_DIR:-${REPO_ROOT}/sglang_profile}"
export PYTHONPATH="${REPO_ROOT}/src${PYTHONPATH:+:${PYTHONPATH}}"
export SGLANG_EXTERNAL_MODEL_PACKAGE="${SGLANG_EXTERNAL_MODEL_PACKAGE:-rwkv7vl_model}"
export SGLANG_EXTERNAL_MM_MODEL_ARCH="${SGLANG_EXTERNAL_MM_MODEL_ARCH:-RWKV7VLForConditionalGeneration}"
export SGLANG_EXTERNAL_MM_PROCESSOR_PACKAGE="${SGLANG_EXTERNAL_MM_PROCESSOR_PACKAGE:-rwkv7vl_processor}"

if [[ -n "${CUDA_LAUNCH_BLOCKING:-}" ]]; then
  export CUDA_LAUNCH_BLOCKING
fi

cd "${REPO_ROOT}"
